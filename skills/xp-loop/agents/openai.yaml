interface:
  display_name: "SuperXP XP Loop"
  short_description: "Internal SuperXP core workflow"
  brand_color: "#2563EB"
  default_prompt: "Use $xp-loop only after SuperXP has been explicitly selected."

policy:
  allow_implicit_invocation: false
