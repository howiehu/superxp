interface:
  display_name: "SuperXP XP Loop"
  short_description: "Minimal XP loop with Test First"
  brand_color: "#2563EB"
  default_prompt: "Use $xp-loop to clarify, slice, verify, and apply Test First."

policy:
  allow_implicit_invocation: true
